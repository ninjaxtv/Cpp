#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
	vector<int> a = {45, 89, 23};
	sort(a.begin(), a.end()); // To sort the items in vector a in ascesding order
	a.push_back(1000); // To add 1000 as the last element in vector (vector a)
	IsThere = binary_search(a.begin(), a.end(), 45); // This will check if in the vector a has 45 or not, The answer will be true
	 
    	
}
